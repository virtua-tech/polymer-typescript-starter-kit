{
  "name": "redir-psk",
  "private": true,
  "dependencies": {
    "prpl-server":"^1.0.0"
  },
  "scripts": {
    "start": "prpl-server --host 0.0.0.0 --port 8081 --config polymer.json"
  },
  "engines": {
    "node": ">=6.0.0"
  }
}